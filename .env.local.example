# ============================================
# IMPORTANT: Copy this file to .env.local and fill in your values
# cp .env.local.example .env.local
# ============================================

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3001
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Google OAuth (Optional - for social login)
# Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create OAuth 2.0 credentials
# 3. Add http://localhost:3001/api/auth/callback/google to redirect URIs
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Apple OAuth (Optional - for social login)
# Instructions:
# 1. Go to https://developer.apple.com/
# 2. Create a Service ID with Sign in with Apple
# 3. Add http://localhost:3001/api/auth/callback/apple to redirect URLs
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# JWT Secret (for custom JWT tokens)
# Generate with: openssl rand -base64 32
JWT_SECRET=

# AWS S3 Configuration (Optional - for file uploads)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Optional: CloudFront Distribution (for faster content delivery)
# AWS_CLOUDFRONT_DOMAIN=

# Google Maps API (Optional - for interactive maps with geocoding)
# Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Enable Maps JavaScript API and Geocoding API
# 3. Create an API key and restrict it to your domains
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=



