import { Github, Chrome as Google, Apple, LucideIcon } from "lucide-react"
import Spinner from "@/components/ui/Spinner"

export type Icon = LucideIcon

export const Icons = {
  spinner: Spinner,
  github: Github,
  google: Google,
  apple: Apple,
}